version: '2'
services:
  web:
    build: .
    command: nginx
    ports:
      - "80:80" 
      - "443:443"
    depends_on:
      - db
    volumes:
      - .:/code
  db:
    image: orchardup/mysql
    environment:
      MYSQL_DATABASE: wordpress
